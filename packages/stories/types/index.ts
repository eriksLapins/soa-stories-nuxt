export type StoryModuleOptions = {
    title?: string;
    subtitle?: string;
    meta?: {
        title?: string;
        subtitle?: string;
    },
    componentsDir?: string; // directory of setup components
}